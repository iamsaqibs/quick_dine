class OrderItem {
  OrderItem(this.id, this.imageURL, this.title, this.subTitle, this.price, this.quantity);
  int id;
  String imageURL;
  String title;
  String subTitle;
  double price;
  int quantity;
}